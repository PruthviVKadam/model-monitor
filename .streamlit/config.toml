# Brand theme — Model Monitor (drift / MLOps). Dark base + alert-red accent.
[theme]
base = "dark"
primaryColor = "#ff6b6b"
backgroundColor = "#0e0f14"
secondaryBackgroundColor = "#171a23"
textColor = "#e6e8ee"
font = "sans serif"

[server]
headless = true
